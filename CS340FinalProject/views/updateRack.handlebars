<h1>Update {{rack_barcode}}:</h1>
<div class = "container">
    <div class = "row">
        <div class = "col-4 mx-auto mt-1" id = "formContainer">
            <form id = "updateRack" action="/rack/{{rack.rack_barcode}}" method="post">
                <h1>Update Rack</h1>
                <p>Please enter the information below to update a rack. </p>
                Rack Barcode: {{rack.rack_barcode}} </br>
                Rack Type Name: </br>
                <select name="rackType">
                  {{#each rackType}}
                    <option value="{{rack_type_id}}">{{rack_type_name}}</option>
                  {{/each}}
                </select> </br></br>
                <button>Update</button>
                </br></br>
                <button><a href="../rack">Back</a></button>
            </form></br>
        </div>
    </div>
</div>
